<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Разрешение для доступа к интернету, необходимо для Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <!-- Активность регистрации, точка входа в приложение -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Активность авторизации -->
        <activity
            android:name=".LoginActivity"
            android:exported="true" />

        <!-- Активность приветствия -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true" />

        <!-- Активность управления услугами -->
        <activity
            android:name=".ManageServicesActivity"
            android:exported="false" />

        <!-- Активность управления пользователями -->
        <activity
            android:name=".ManageUsersActivity"
            android:exported="false" />

        <activity
            android:name=".ViewLogsActivity" android:exported="false" />

    </application>

</manifest>